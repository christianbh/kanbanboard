<h1>Information for <%= @task.name %></h1>

<p>
  <b>Name:</b>
  <%=h @task.name %>
</p>
<p>
  <b>Requested by / Contact:</b>
  <%=h @task.requester %>
|
  <b>Created:</b>
  <%=h @task.created_at.strftime("%b %d %Y") %>
|
<% if @task.state != State::NEW %>
<% if @task.documentation%>

    <b>URL to documentation:</b> <a href="<%= @task.documentation %>"> <%= @task.documentation %></a>

<% else %>

    <b>URL to documentation:</b> <%= link_to 'Edit', edit_board_task_path(@board, @task) %> to add link to documentation

<% end %>
<% end %>
</p>
<div class="info">
  <b>Description:</b>
  <%=h @task.description %>
</div>
<div class="info">
  <b>How does this add value:</b>
  <%=h @task.value %>
</div>
<div class="info">
  <b>Consequence if not done:</b>
  <%=h @task.consequence %>
</div>


<%= link_to 'Edit Task', edit_board_task_path(@board, @task) %> |
<%= link_to 'Back to Board', board_path(@board) %> |
<hr/>
<p>
	<b>Task completed?</b><br/> 
	Set task to done: <%= button_to 'Done', done_board_task_path(:board_id => @board, :project => @task), :confirm => 'This will set the status of this task to done. Are you sure?', :method => :done %> 
</p>
<hr/> 
<b>No longer relevant?</b> Delete this task: <%= link_to 'Destroy', board_task_path(@board, @task), :confirm => 'Are you sure?', :method => :delete %>
